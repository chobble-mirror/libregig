<% content_for :title, @band.name %>

<% content_for :nav do %>
  <%= render 'band_nav', band: @band %>
<% end %>

<% case @view %>
<% when "overview" %>
  <%= render "show_overview", band: @band %>
<% when "shares" %>
  <%= render "show_shares", band: @band %>
<% end %>
