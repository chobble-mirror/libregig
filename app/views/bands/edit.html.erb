<% content_for :title, @band.name %>

<% content_for :nav do %>
  <%= render 'band_nav', band: @band %>
<% end %>

<section>
  <h1 class="my-4">
    <%= @band.name %>
  </h1>
  <%= render 'form', band: @band %>
</section>
